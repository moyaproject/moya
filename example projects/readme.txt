This directory contains some example projects.

To test them, go in to the project directory and run the following:

    moya db sync
    moya auth#cmd.init
    moya runserver

You will only need to first two commands once only.

There may be a readme.txt for each project.
